setwd("C:/C-epfl results/Options/aapl/")

#data <- read.csv("aapl.csv")

# unique dates
dates_unique <- unique(data$date)

one_day <- data[ (data$date == dates_unique[3]) & (data$cp_flag == "C") & (data$exdate > 20140404), ]

one_day$strike_price

unique( one_day$exdate )

library("scatterplot3d")
scatterplot3d( x=one_day$strike_price, y=one_day$exdate, z=one_day$impl_volatility ) 